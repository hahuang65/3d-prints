# Render view for the Volvo Headrest Cupholder.
# Lots of curved surfaces — raise the edge threshold so cylinder facet
# boundaries don't get inked as edges (keeps the cup rims and silhouette).
EDGE_THRESHOLD=22
# Drop the ghost headrest-neck visualisation so only the printed part shows.
DEFINE=show_leg=false
