# Render view for the Caterham Tunnel Tray.
# The scad defaults to MODE="test_plate" (a flat 1mm fit-test coupon); override
# it to the real assembled tray. Camera looks slightly down into the tray so the
# walls, floor, and wiring-tube notch all read.
DEFINE=MODE="assembled"
CAMERA=58,0,25
