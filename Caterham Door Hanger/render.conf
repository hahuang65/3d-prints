# Render view for the Caterham Door Hanger.
# The model is authored flat (print orientation); rotate it 90° about X so it
# reads as a wall-mounted bracket — backplate vertical against the wall, the
# two J-hooks curving down and forward. Colour is inherited from render.sh's
# default (Catppuccin Mocha).
ROTATE=90,0,0
CAMERA=62,0,37
